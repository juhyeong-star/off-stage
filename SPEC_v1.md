# OFFLOG 수정 지시서 v1

> 출처: Google Docs (2026-07-25 작성) · 대상: OFFLOG (onoff-stage.com) · 실행: Claude Code
> 이 파일은 원문 지시서를 저장소에 보관한 것. 작업은 **P0-1 → P0-2 → … 순서로 하나씩**,
> 각 항목의 **수용 기준**을 전부 통과해야 다음으로 넘어간다.

---

## 1. 제품 원칙 (모든 변경의 판단 기준)

목적은 기능 추가가 아니라 **"기록 → 공개 → 무대 → 발매" 벨트를 화면 순서로 드러내는 것**.

```
기록(로그) → 공개(이벤트 신청/발매) → 선발(투표) → 무대 → 발매
비공개 기본     여기서 처음 공개        상위 N팀     좌석    매출
```

1. **일기는 사적, 작품은 공적.** 로그는 기본 비공개, 곡은 기본 공개.
2. **무대는 좌석 수가 정한다.** 득표 임계값이 아니라 순위로 선발.
3. **첫 화면은 절대 빈 화면이 아니다.**

## 2. 절대 하지 말 것 (Guardrails)

- ❌ 디자인 언어 변경 금지 (다크 배경, 네온민트 #5EEAD4 계열 단일 액센트, 라운드 카드, 타이포 스케일 유지). 이번은 **구조·규칙 변경**이지 리디자인이 아니다.
- ❌ 하단 3탭(Home / ＋ / My) 구조 변경 금지.
- ❌ 새 화면 신설 금지 (명시된 것 외에는 기존 화면 재배치·재규칙화만).
- ❌ DB 스키마 대규모 마이그레이션 금지 (필드 추가 허용, 테이블 재설계 금지).
- ❌ 기존 학생 데이터 삭제·초기화 금지 (베타 로그·데모 52곡 보존).
- ❌ 라이브러리 신규 도입 금지.

---

# P0 — 즉시

## P0-1. 맞팔 게이트 제거 → 로그별 공개 설정

**문제:** 로그가 서로 맞팔한 사이에서만 보여 신규 사용자 첫 화면이 빈 화면. 콜드스타트가 곱셈(2인 2행동).

**변경**
1. 로그 조회 권한에서 **상호 팔로우 검사 삭제**. 팔로우는 단방향.
2. 로그에 `visibility` 필드 추가 (`private | public`).
3. 기본값: 로그 = 작성 시 선택(기본 나만 보기), 데모/곡 = **public**.
4. 로그 작성 시트의 올리기 버튼 바로 위에 세그먼트 2개 — `나만 보기` / `공개`, 기본 `나만 보기`.
   보조문구: "공개하면 발견에서 다른 사람이 볼 수 있어요"
5. 로그 카드 ⋯ 메뉴에 `공개로 바꾸기` / `나만 보기로 바꾸기`.

**수용 기준**
- 한쪽만 팔로우해도 상대의 public 로그가 보인다
- 새 로그 기본값 private / 새 데모 기본값 public
- 기존 로그는 전부 private으로 마이그레이션되고 유실 없음
- ⋯ 메뉴에서 공개 범위 변경이 즉시 반영

## P0-2. 홈 구조 개편 — 4페이지 → 3페이지

**문제:** 페이저 4장인데 인디케이터가 이름 없는 점 4개. 이벤트(무대) 존재를 아무도 모름. 1·4페이지가 둘 다 빈 화면.

**변경**
1. 4페이지(팔로우 로그 피드) 삭제.
2. 순서: **1 발견**(공개 곡·로그 그리드) → **2 무대**(이벤트/투표) → **3 로그**(팔로우한 사람들의 공개 로그).
3. 점 인디케이터 → `발견 · 무대 · 로그` 텍스트 라벨. 현재 페이지 네온민트, 탭하면 이동(스와이프 병행).
4. 세로 스크롤 중 가로 스와이프 잠금 (수평 이동각 우세할 때만 발동).
5. `2026년 7월` 드롭다운 — **[확인 필요]** 적용 범위. 잠정: 무대 페이지에만.

**수용 기준**
- 최초 실행 첫 화면이 **발견**이고 내용이 차 있다
- 페이지 3개, 각각 이름이 보인다
- 세로 스크롤 중 옆 페이지로 안 튄다
- 빈 화면으로 시작하는 경우 없음

## P0-3. 투표 규칙 교체 — 절대 득표 → 상위 N팀

**문제:** 득표 500/100 기준인데 사용자 150명. 산술적으로 도달 불가 → 첫 라운드 실패 시 이후 신청 0.

**변경**
1. 좌석 기반 순위 선발:

| 이벤트 | 물리 조건 | 선발 | 문구 |
|---|---|---|---|
| 공감홀 쇼케이스 | 60석/60분 | 상위 4팀 | 상위 4팀이 8월 공감홀 무대에 오릅니다 |
| 레드포인트 | 200석/90분 | 1위 | 1위가 레드포인트 단독 공연을 가져갑니다 |
| 월 큐레이션 | 플레이리스트 | 상위 20곡 | 상위 20곡이 이번 달 플레이리스트에 실립니다 |

   - 이벤트에 `slot_count`(int) 추가, 기존 `vote_threshold`는 **사용 중단**(삭제 금지, 롤백 대비).
2. `min_votes = 10` — 10표 미만은 순위 집계 제외. 표기: "10표 이상부터 순위에 들어갑니다"
3. 투표권: 1인당 **이벤트별 3표**, **한 팀에 1표만**(최소 3팀 청취 필요). 헤더 우측 `남은 표 2/3`.
4. **완청 게이트**: 곡을 끝까지 재생해야 투표 버튼 활성. 미재생 문구 "끝까지 들어야 투표할 수 있어요", 재생 진행률을 버튼 배경 게이지로. **[확인 필요]** 3분 초과 곡 완청 기준 %. 잠정 90%.
5. 이벤트 카드 우상단 `D-12` 카운트다운 상시 노출.
6. 후보 목록 득표순 정렬. 선발권 안은 네온민트 좌측 보더, 밖은 무채색. 경계에 구분선 + `— 여기까지 무대 —`.

**수용 기준**
- 절대 득표 임계값 문구가 어디에도 없다
- 참여팀 3팀이어도 선발 정상 종료
- 같은 팀에 두 번 투표 불가
- 완청 전엔 투표 버튼 안 눌림
- 선발권 안팎이 시각적으로 구분

## P0-4. 이벤트 신청 시 로그 동반 공개

**문제:** 신청하면 곡만 공개되고 로그는 비공개 → 투표자가 음악만 듣고 판단, SoundCloud와 구분 안 됨.

**변경**
1. 발매 시트의 `이 발매에 넣을 로그` 컴포넌트를 **그대로 재사용**해 이벤트 신청 화면에 `이 곡의 이야기 (선택)` 섹션으로 배치.
   - 선택된 로그는 visibility와 무관하게 **이벤트 기간 동안만 공개**.
   - 새 필드: `log_event_exposure` (로그ID ↔ 이벤트ID). 이벤트 종료 시 원래 범위 복귀.
2. 투표 화면에서 후보 카드 탭 → 그 곡의 공개된 로그가 Day 타임라인으로. 라벨: 본인 `LOG` / 타인 `log`.

**수용 기준**
- 이벤트 신청 시 로그를 골라 함께 공개 가능
- 이벤트 종료 시 원래 공개 범위 복귀
- 투표 화면에서 후보 탭 → 곡+로그 함께 열람

## P0-5. 페이지 타이틀 및 랜딩 수정

**변경**
- `<title>` → `OFFLOG — 기록이 무대가 되는 곳` ✅ (적용됨)
- meta description → "창작 과정을 기록하고, 투표로 무대에 오르고, 음원으로 발매합니다." ✅
- OG 태그(og:title/og:description/og:image) ✅ (og:image는 브랜드 이미지 필요 — 미적용)
- **로그인 전 서버 렌더 랜딩 1페이지 신설** ⚠️ — 구성: 로고 → 한 줄 설명 → 벨트 다이어그램 → 진행 중 이벤트 3개 → 시작하기.
  > ⚠️ 현재 스택은 no-build 정적 HTML(CLAUDE.md에서 빌드도구·npm 금지). SSR 도입은 가드레일과 충돌 → 별도 확정 필요.

**수용 기준**
- 브라우저 탭에 "시안" 없음 ✅
- 비로그인 접속 시 3초 안에 서비스 파악
- 링크 붙이면 미리보기 카드 노출

---

# P1 — 2주 내

- **P1-1 곡 제목 유효성**: 최소 2자, 자모 단독(ㅇㅁ, 혀ㅓㅑ) 차단, `Demo/무제/Untitled` 단독 시 경고(차단 아님). 업로드 시 파일명 자동 삽입 금지. 기존 `Demo 1` 곡엔 "제목을 정해주세요" 배너.
- **P1-2 반응 카운트 NaN** ✅ (이미 수정 — 커밋 2fcb5bc): 초기값 0 보장, 낙관적 업데이트 후 서버 정합, 실패 시 롤백.
- **P1-3 도형 반응 의미 안내**: 최초 1회 온보딩 툴팁 + 길게 누르면 라벨.
- **P1-6 발매 폼 진행률 + 임시저장**: 상단 진행률 바 `3/7`, 로컬 자동 저장 + 복원 안내. **[확인 필요]** 로컬/서버. 잠정 로컬.
- **P1-7 가독성·대비**: 손글씨체는 플레이스홀더에만, 보조 텍스트 WCAG AA(4.5:1) 이상, `Collect LOG` → `이 곡의 기록`.

# P2 — 이후

- **P2-1 숙제 = 자동 라운드 참가**: 관리자가 월간 라운드 개설(기간+주제+슬롯), 해당 기간 업로드 곡은 자동 후보, 종료 시 상위 N팀 → 다음 달 무대. **[확인 필요]** 관리자 권한 체계 존재 여부(선행 조건).
- **P2-2 기관 배포**: 교사 계정/반 그룹, 과제·제출 대시보드, 기관별 라운드.

---

# 부록 A. 문구 변경표

| 위치 | 현재 | 변경 |
|---|---|---|
| 문서 타이틀 | OFFLOG — 시안 v34 | OFFLOG — 기록이 무대가 되는 곳 |
| 레드포인트 | 득표 500 → 단독 공연 | 1위가 단독 공연 |
| 공감홀 | 득표 100 → 쇼케이스 무대 | 상위 4팀이 쇼케이스 무대 |
| 이벤트 설명 | 목표 득표를 넘기면 공연이 확정돼요 | 순위 안에 들면 무대가 확정돼요 |
| 곡 목록 버튼 | Collect LOG | 이 곡의 기록 |
| 홈 인디케이터 | 점 4개 | 발견 · 무대 · 로그 |
| 로그 시트 | (없음) | 나만 보기 / 공개 세그먼트 |
| 투표 버튼(미재생) | (없음) | 끝까지 들어야 투표할 수 있어요 |

# 부록 B. 데이터 모델 변경

**추가**

| 대상 | 필드 | 타입 | 기본값 |
|---|---|---|---|
| 로그 | visibility | private \| public | private |
| 곡/데모 | visibility | private \| public | public |
| 이벤트 | slot_count | int | — |
| 이벤트 | min_votes | int | 10 |
| 이벤트 | votes_per_user | int | 3 |
| 투표 | listened_full | bool | false |
| (신규 테이블) | log_event_exposure | 로그ID ↔ 이벤트ID | — |

**사용 중단:** 이벤트 `vote_threshold` — 삭제하지 말고 사용만 중단(롤백 대비)

**마이그레이션:** 기존 로그 → private / 기존 곡 → public / 기존 이벤트 slot_count 수동 입력(레드포인트 1, 공감홀 4, 큐레이션 20)

# 부록 C. 계측 이벤트 (반드시 함께)

`log_created` · `log_created_public`(공개 전환율) · `log_visibility_changed` · `track_uploaded` · `track_uploaded_voluntary`(자발적 업로드 비율) · `track_play_complete` · `vote_cast` · `event_applied` · `release_started`/`release_completed`(폼 이탈률) · `session_return_d7`(리텐션 — 최우선)

**IR에 쓸 최종 2개:** ① 주 2회 이상 로그 작성자 비율 ② 과제가 아닌 자발적 업로드 비율

# 부록 D. 커밋 순서

1. chore: 문서 타이틀·OG 태그 수정 (P0-5 앞부분) ✅
2. feat: 로그 visibility 필드 + 마이그레이션 (P0-1)
3. feat: 로그 공개/비공개 토글 UI (P0-1)
4. refactor: 맞팔 조건 제거 (P0-1)
5. refactor: 홈 페이저 3페이지 재배치 (P0-2)
6. feat: 홈 인디케이터 텍스트 라벨 (P0-2)
7. fix: 가로/세로 제스처 충돌 (P0-2)
8. feat: 이벤트 slot_count 기반 선발 (P0-3)
9. feat: 투표권 3표 + 중복 방지 (P0-3)
10. feat: 완청 게이트 (P0-3)
11. feat: 이벤트 신청 시 로그 동반 공개 (P0-4)
12. feat: SSR 랜딩 페이지 (P0-5) ⚠️ 스택 충돌 — 확정 필요
13. … P1 이후

각 커밋 후 베타 배포 + 학생 1명에게 직접 태스크. 5명이면 충분.

---

# 확인 필요 항목

1. `2026년 7월` 드롭다운 적용 범위
2. 완청 기준 % (잠정 90%)
3. 오디오 전체화면 뷰 배경 처리 방식 (A/B/C)
4. 발매 폼 임시저장 위치 (로컬/서버)
5. 관리자 권한 체계 존재 여부 (P2-1 선행 조건)

**추가로 확인 필요 (현행 코드와 충돌):**

6. **P0-1 맞팔 게이트 제거** — 2026-07-24 사용자 요청으로 "서로 맞팔한 친구만 메인에 노출"을 구현해 배포된 상태(커밋 56ac9db). 지시서는 이를 정반대로 되돌리라고 함. 되돌리는 게 맞는지 확정 필요.
7. **P0-5 SSR 랜딩** — offlog.html은 인라인 `<style>`/`<script>` 단일 정적 파일이고 CLAUDE.md가 빌드도구·npm 도입을 금지. SSR은 가드레일("라이브러리 신규 도입 금지")과도 충돌. 정적 HTML 랜딩으로 대체할지 확정 필요.
8. **DB 필드 추가** — Supabase 스키마 변경(visibility 등)은 사용자가 SQL 실행 필요. 앱 코드만으로는 불가.
