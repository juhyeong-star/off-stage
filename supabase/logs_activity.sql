-- =====================================================================
-- OFFLOG — 로그 활동 태그 (📊 활동 분석표용, 요청 10)
--
-- 로그에 '활동 분류'(연습/공연/작업/기타)를 저장하는 컬럼 하나를 추가합니다.
--   prac=🎸연습 · show=🎤공연 · work=🎧작업 · etc=📌기타 · null=미분류
--
-- 실행 안 해도 앱은 정상 동작합니다(활동 태그가 이 기기에만 저장될 뿐).
-- 실행하면: 활동 태그가 계정에 영구 저장 → 기기를 바꿔도 분석표가 그대로.
--
-- 실행법: Supabase 대시보드 → SQL Editor → 붙여넣고 Run (한 번만, 멱등)
-- =====================================================================

alter table public.logs add column if not exists activity text;

-- (기존 RLS 정책 그대로 사용 — 새 정책 필요 없음)
